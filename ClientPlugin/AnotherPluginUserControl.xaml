<UserControl x:Class="ClientPlugin.AnotherPluginUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClientPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Button Click="Button_Click">Send WndProc Message manually (uses Native method so should fail)</Button>
        <Button Click="Button_Click_1">Test writing a file (IO is restricted so should fail)</Button>
    </StackPanel>
</UserControl>
